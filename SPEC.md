# Vault Feed Reader — MVP SPEC

狀態：2026-09-22 訪談結論；使用者已確認 Codex subagents 與實作計畫模型分配，MVP 核心實作與主要流程 smoke 已完成；未驗證情境詳見 docs/history/2026-09-22-rss-reader-mvp/mvp-validation.md。以下是驗收目標，完成情況以驗收記錄為準。歷史對話不覆蓋本文件的最終決策。

## S1 平台與範圍

Obsidian 桌面插件，獨立 RSS／Atom reader。只抓可直接透過 HTTP(S) URL 取得的 feed；不處理帳號登入、Cookie、特殊驗證、Feedly 帳號、reader 內全文網頁擷取、附件下載、tags、巢狀資料夾、全文搜尋。vault 如何同步或迁移不在範圍內。插件需正確讀寫自身資料並處理本機檔案變更，不建跨裝置衝突合併系統。

## S2 訂閱

vault 內可設定路徑的 YAML 是唯一訂閱權威來源。介面 CRUD 回寫 YAML；支援 YAML／TOML／OPML 匯入與匯出（YAML 匯出沿用使用者最初要求）。匯入預設合併、以來源 URL 去重；取代需明確確認。合併保留既有來源 ID、閱讀紀錄與資料夾關聯，不把來源 title 當識別。

僅單層資料夾，一個來源可屬多個資料夾。同一來源只有一份文章、快取及已讀狀態。從資料夾移除只是解除關聯；取消訂閱是全域移除來源與本機快取，但保留閱讀紀錄及筆記。刪除資料夾保留訂閱，無任何資料夾的來源出現在「未分類」。重新訂閱同一來源恢復原有已讀判定。

外部修改 YAML 自動載入；無效語法、schema 或重複 ID 不得部分套用。顯示可定位的錯誤，沿用最後有效清單並停止 UI 回寫，直到檔案修正。啟動時沒有有效版本則呈現空清單與錯誤，不以空檔覆寫損壞檔案。有效 UI 寫入必須序列化，避免同時操作互相遺失。

共用形狀见 `src/domain/models.ts`。實作預設：時間用 UTC ISO 字串；ID 穩定且可安全作檔名；URL 正規化保留 query（可能含 token），不把不同 query 合併。不要將私人訂閱 URL 寫進提交的 fixtures 或紀錄。

## S3 資料與閱讀狀態

- 訂閱 YAML、按來源分檔且帶版本的已讀 JSON、Markdown 筆記都放 vault。
- 文章快取放本機，不能假設 `.obsidian/plugins/` 不會同步。建議 IndexedDB，按 vault 身分與 feed ID 分區；可丟棄重建，不需 SQLite。
- 已讀 JSON 預設目錄 `Feed Reader/state/`；subscriptions 預設 `Feed Reader/feeds.yaml`；保存目錄預設 `Feed Reader/Articles`。後兩項可配置，狀態目錄 MVP 固定即可。
- 每来源存 `readBefore`、`readIds`、`unreadIds`。判定順序：未讀例外 → 個別已讀 → 文章時間早於截止 → 未讀。文章時間採有效發佈時間，缺少則首次抓取時間。
- 「此日期以前全部已讀」以本地日期起點轉 UTC，使用嚴格早於；「全部標已讀」截止為操作當下，當前已知同時刻文章另入個別已讀，避免等號邊界漏掉。來源／資料夾／全域操作影響對應來源，與文字搜尋無關。
- 推進截止時清掉涵蓋的未讀例外及可壓縮的個別已讀；已無 metadata 的 IDs 可保留，正確性優先，不建背景壓縮系統。後補的舊文亦算已讀。
- 手動標未讀覆蓋 cutoff，更新、重啟與快取淘汰均保留例外；開啟、手動已讀或再次涵蓋的全已讀操作可清除例外。
- 初次訂閱所有現有文章未讀。移除來源仍保留狀態。破損狀態檔提示錯誤且不靜默覆寫，寫入失敗不能呈現成功。

## S4 取得、識別、快取

RSS／Atom 使用成熟解析工具處理 namespaces；優先 GUID／Atom ID，再用原文 URL，最後穩定欄位組合。ID 在同一來源內穩定，跨來源不合併。首次抓取時間在更新時保持不變。既有文章內容更新只更新快取，不自動轉未讀、不改保存筆記。

每來源最多 500 篇，依有效文章時間由新到舊保留；淘汰不修改閱讀狀態或保存筆記。已讀／未讀／今日只呈現仍有快取的文章，不是歷史檔案館。已保存檢視則可直接列保存筆記，即使文章快取已淘汰（實作預設，避免保存入口失去用途）。

開啟 reader 更新，任一 reader 分頁存在期間每 30 分鐘更新，提供手動更新；切去筆記但分頁仍存在時繼續。最後 reader 關閉後停止；恢復休眠過期只補一次。限制併發（起始 4）、請求逾時、來源間隔離錯誤、同來源不重複在途抓取；失敗保留已有內容並顯示來源錯誤。unload 清理 timer/listener，避免遲到結果更新已卸載 UI。

200 來源 × 500 篇為效能目標，不在啟動載入所有全文。列表分頁／增量讀 metadata，文章全文按需讀取。

## S5 介面與鍵盤

ribbon RSS 按鈕同時開左側來源與主區域 reader；重複開啟復用既有葉節點。依 2026-09-22 後續使用者要求，主區域改為列表→全文兩種畫面：點來源／資料夾先顯示全寬列表，點文章進入全文；提供返回列表與上一篇／下一篇。支援外部瀏覽器開原文。點來源／資料夾或開啟閱讀器時，列表預設套用未讀（unread）篩選；全域設定提供「預設列表篩選」（defaultListFilter），可調整為未讀（unread）、全部（all）、已讀（read）或今日（today）。

列表在選擇資料夾或全域入口時維持簡單 list 版面（來源、標題與發布日期）；選擇單一 RSS Feed 時改為詳細資訊＋縮圖版面（雜誌樣式，依發布日期群組，呈現縮圖、標題、作者／時間與內文摘要）。

全域有全部、已讀、未讀、今日、已保存入口；來源和資料夾可套用已讀／未讀／今日與標題搜尋。今日依本地時區發佈日，缺日期用首次抓取日，預設日期降序。同來源多資料夾不造成全域文章重複。

`j/k` 移動文章，內容開啟時同步切換；`Enter` 開內容；`o` 開原文；`m` 切換已讀；`s` 保存／開筆記；`Esc` 返回列表。只在 reader 有焦點且非 input、textarea、select、contenteditable 或輸入法組字時處理，無選取文章則安全不動作。

開內容即已讀；`markReadOnNavigate` 預設 false，啟用後 j/k 移入即已讀。未讀列表保留剛讀過的文章直到使用者重新套用篩選或切換來源，背景更新不應破壞此操作穩定性。UI 必須有新增／編輯／取消訂閱、分類關聯、匯入匯出、手動更新及批次已讀操作入口，不只做 service。

### 介面多語系

提供英文（en）與繁體中文（zh-TW）。語言設定預設 auto，插件啟動時跟隨 Obsidian；繁體中文區域／字體代碼使用繁體中文，其餘未支援語系使用英文。可明確選擇英文或繁體中文，設定寫入失敗時恢復原選擇；成功後於停用並重新啟用插件時套用，既有設定草稿不因選擇語言而重繪清除。

翻譯涵蓋 reader、管理頁、設定、命令及操作提示；日期顯示採用介面語系。來源與文章內容、使用者名稱、筆記模板、提示詞、路徑、資料欄位與命令 ID 保持原值。底層解析器、儲存或外部 CLI 的技術診斷可保留原文。

## S6 保存与內容安全

reader 的 Save 保存 RSS 正文或摘要為 Markdown，不額外抓網頁；獨立筆記命令見 S8。無內容仍存標題、連結等 metadata 並明示來源沒有內容。圖片保留遠端 URL；可離線讀文字，圖片不保證。內容渲染及轉換前使用可靠 sanitization，禁止 script、iframe、事件 handler 與危險 URL scheme；相對圖片與連結按來源 URL 解析，只開安全 HTTP(S) 原文。

檔名為日期＋安全化標題，重名加短識別碼；不覆蓋其他筆記。frontmatter 至少含 `feed_reader_id`（feedId＋articleId 的穩定組合）、`feed_reader_source_id`、來源名稱、原文網址、發佈時間（若有）、保存時間。透過 serializer 處理 YAML escaping。

重複 Save 開啟已存在筆記、不覆寫人工內容；同文章同時保存需去重。只有成功寫入後才記錄保存狀態；失敗可重試。筆記刪除後可重建。用可重建路徑索引＋vault rename/delete 事件追蹤；索引路徑失效時才查 metadata cache，不反覆讀全 vault 文字。取消訂閱不刪保存筆記。

## S7 驗收

1. 建置成功，Obsidian 載入／停用／重新開 reader 正常，設定重啟保留。
2. YAML/TOML 往返保留來源與分類；合併、取代、外部修改與無效檔案均符合 S2，重複 URL 不丟分類。
3. RSS 與 Atom fixtures 可解析；重抓、更新、缺欄位、快取淘汰和重新訂閱不造成身分或已讀錯亂。
4. cutoff、個別已讀、未讀例外、同時刻邊界、重啟、寫入失敗皆有針對性測試。
5. UI 可完成新增→抓取→閱讀→標未讀→保存→重開笔記；快捷鍵焦點、篩選穩定性及多資料夾一致。
6. 惡意 HTML 不執行；重複保存、同名不同文、移動／刪除筆記與寫入失敗皆有覆蓋。
7. 合成 100,000 metadata 的檢查記錄列表載入時間和全文讀取數，確認無一次載入全量 DOM／全文；不宣稱未量測的速度承諾。
8. 測試 vault 實機檢查 scheduler、離線錯誤及 unload；記錄版本與結果，不將單元測試當成實機通過。

## S6 補充：可設定筆記模板（2026-09-22）

全域設定提供檔名模板、Markdown 內文模板與自訂 Properties YAML，以及範例資料即時預覽。套用前驗證未知／破損變數、格式及 YAML。Properties 先解析結構再代入值，最後統一 serialize；保留 title、date_created、date_updated 與 feed_reader_*，不得破壞筆記識別。日期格式採 UTC，date 提供日期 fallback。模板只作用於新筆記，既有筆記去重、人工編輯保護及 rename/delete 追蹤保持原有語意。第一版不新增作者／摘要欄位解析、不執行腳本、不提供每来源個別模板。


## OPML 交換格式（2026-09-22）

OPML 匯入／匯出沿用訂閱管理頁及 merge／replace 流程，不改變 YAML 權威來源。接受版本 1.0／1.1／2.0，匯出 2.0。巢狀 outline 以完整分類路徑攤平為單層名稱；保留父分類、空分類與同來源多分類關聯。匯入按 URL 合併，同名分類對應現有分類；merge 保留既有名稱及關聯，replace 使用匯入內容，兩者都沿用已知 URL 的來源 ID。分類歧義與無效內容整次拒絕，不部分套用。OPML 不交換閱讀狀態、快取、筆記或本專案內部 ID。詳細操作與限制見 README。

## S8 筆記全文與本地 Agent 摘要（2026-09-22）

已確認契約：[完整規格](docs/history/2026-09-22-article-enrichment/spec.md)。本節擴充 S1/S6，不改 reader 的 Save、閱讀狀態或快取。

- Obsidian 命令面板提供「抓取原文全文」「產生 AI 摘要」「抓取全文並摘要」，僅對目前 Markdown 筆記執行。
- 公開 HTTP(S) HTML 經 Readability 與共用清理規則轉 Markdown；不登入、不執行 JS、不用 proxy。不以取得 HTML 或抽出文字保證網站提供完整文章。
- 網址欄位可設定，預設 feed_reader_url/source/url；多個不同有效網址選擇，缺少則提示補入。
- 原文規則支援多組有序標題及 Properties 條件整正文。第一匹配優先，重複區塊選擇；新內容使用管理標記。既有原文範圍取代（包括其人工編輯），否則附加；範圍外與 Properties 保留。
- 摘要排除 Properties 與已有摘要，全文不明時自動背景擷取，單獨摘要命令只寫摘要，不寫全文。預設繁中概述＋3–5 重點，可自訂提示詞；AI 摘要區塊放在文章前並於重跑取代。組合命令任一步失敗不寫部分結果。
- Codex/Claude Code/OpenCode/pi 內建與自訂 CLI；提供偵測、預設、路徑、完整參數、測試。模型與登入沿用 CLI；插件不提供安裝、登入或模型服務。設定測試使用非筆記測試文字，仍可能消耗模型額度。
- 本機 CLI 設定使用 vault 專用 localStorage；原文規則／提示詞使用插件 data.json。CLI 以暫存 cwd、stdin、shell:false 執行，插件掌管筆記更新；不是全面 OS 沙箱，仍信任全域 CLI/MCP 設定及自訂參數。
- 同篇單工作、背景取消、固定起始筆記；卸載取消。筆記有變動時先預覽供複製／取消／明確取代。開啟筆記經 editor 更新並由 Obsidian 自動保存，關閉筆記用 vault.process 原子比對。
- 擷取 30 秒／下載後 5 MiB 解析上限，CLI 120 秒／2 MiB 輸出上限。取消保證不遲到寫入，不保證底層 requestUrl 終止。Windows .cmd shim 需另用原生執行檔或 wrapper。

驗證與已知限制見 [validation](docs/history/2026-09-22-article-enrichment/validation.md)，未執行實機或模型互通不視為已通過。

## S2/S6 補充：新增來源對話框與日期 Properties（2026-09-22）

新增來源改用 Modal，保留管理列表；Title 可省略並嘗試從 RSS/Atom 的 feed-level title 取得。手填 title 優先，擷取失敗保留草稿並要求補入標題；取消／關閉後不得因晚到網路結果新增訂閱。送出不可重複，成功沿用 SubscriptionService 身分與分類規則。

新保存筆記的 date_created/date_updated 初始為保存時 UTC ISO 日期。移除新輸出的 feed_reader_saved_at/feed_reader_first_fetched_at；索引仍支援舊欄位，firstFetchedAt 缺少時以建立時間作顯示 fallback，不改來源已讀狀態。全文／摘要寫入時更新 date_updated，保留 date_created（舊筆記由 feed_reader_saved_at 遷移），並移除舊兩欄。只作用具 feed_reader_id 的筆記，不改 Web Clipper 自訂日期；不批次遷移／不監聽人工編輯。這些日期列為保留 Properties，重複 Save 仍不改筆記。

## S8 補充：摘要取文與原內容保留（2026-09-22）

「產生 AI 摘要」無可辨識全文時，自動背景抓取原文作為 AI 輸入，只寫入摘要，不附加全文、不再詢問本次使用正文。「抓取原文全文」及「抓取全文並摘要」仍寫入全文。

新摘要放在 Properties 後、原內容前；新全文附加於原內容後。已有管理標記或設定標題所辨識的對應區塊才取代，未知區塊、原始剪藏正文與個人筆記保留。整正文 Properties 規則僅代表摘要輸入，不作為整份筆記覆寫範圍。既有標題區塊仍以至下一同級／高級標題為邊界，區塊內子標題視為該區內容；隱藏管理標記則提供精確邊界。

## S8 補充：AI 主題標籤（2026-09-22）

單獨摘要與全文加摘要命令於同次 CLI 請求產生摘要和 3–5 個主題 tags，以 JSON summary/tags 回傳。驗證後摘要及標籤原子套用：tags 與既有文字／文字清單合併、忽略大小寫去重，不刪既有標籤；無 Properties 則建立。保留其他 Properties 與非目標正文。格式錯誤／既有 tags 不是文字或文字清單時保留原筆記並提示，不部分寫入。單獨擷取全文不生成 tags；摘要為背景擷取時仍不寫全文。
